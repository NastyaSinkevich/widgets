import TooltipItem from '@/components/common/TooltipItem.vue'

import { action } from '@storybook/addon-actions'

export default {
  component: TooltipItem,
  title: 'Tooltip Item',
  tags: ['autodocs']
}

export const TooltipItemDefault = {
  args: {}
}
